#==========================================
# Nicole's Docker Compose Configuration
#==========================================


# version: '3.8'

# services:
#   zookeeper:
#     image: confluentinc/cp-zookeeper:7.6.0
#     environment:
#       ZOOKEEPER_CLIENT_PORT: 2181
#       ZOOKEEPER_TICK_TIME: 2000 
#     ports:
#       - "2181:2181"

#   kafka:
#     image: confluentinc/cp-kafka:7.6.0
#     hostname: kafka-broker
#     depends_on:
#       - zookeeper
#     ports:
#       - "9092:9092"
#     environment:
#       KAFKA_BROKER_ID: 1
#       KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
#       KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT
#       KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,PLAINTEXT_INTERNAL://kafka-broker:19092
#       KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT_INTERNAL
#       KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1 
#       KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
#       KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1 
    

#   kafka-ui:
#     image: provectuslabs/kafka-ui:latest
#     ports:
#       - "8080:8080"
#     environment:
#       KAFKA_CLUSTERS_0_NAME: local
#       KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka-broker:19092 
#       KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181


services:
  
  kafka:
    image: apache/kafka:3.7.0
    container_name: local_kafka
    ports:
      - "9092:9092"   # Traffic inside the internal Docker Network
      - "9094:9094"   # Traffic from outside via ngrok
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@localhost:9093'
      KAFKA_LISTENERS: 'PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093,EXTERNAL://0.0.0.0:9094'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_LOG_DIRS: '/tmp/kraft-combined-logs'
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://kafka:9092,EXTERNAL://localhost:9094'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT'

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka_dashboard
    ports:
      - "8080:8080" # Open http://localhost:8080 in web browser to access the Kafka UI
    environment:
      DYNAMIC_CONFIG_ENABLED: 'true'
      KAFKA_CLUSTERS_0_NAME: 'local-cluster'
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: 'kafka:9092'
    depends_on:
      - kafka

  local-postgres:
    image: postgres:16-alpine
    container_name: local_postgres
    ports:
      - "5432:5432" # Exposed to PC for DBeaver (Database Management Tool similar to pgAdmin)
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mylocaldb

  django-consumer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: django_consumer
    command: python manage.py run_consumer
    environment:
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - DATABASE_URL=postgres://myuser:mypassword@local-postgres:5432/mylocaldb # What should this be?
    depends_on:
      - kafka
      - local-postgres

  # A TCP Tunneling Engine
  ngrok:
    image: ngrok/ngrok:latest
    container_name: local_tunnel
    restart: unless-stopped
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    command:
      - "tcp"
      - "kafka:9094" # Directs raw binary TCP traffic straight to the Kafka container
    depends_on:
      - kafka
